/**
 * Each side keeps at most this many of the passages context selection chose,
 * so both accounts fit one generation within the eight labels the provider
 * contract allows.
 */
export const sidePassageCap = 4
