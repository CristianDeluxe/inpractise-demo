/** A record of the size the ask path actually writes. */
export function diagnosticsPayload() {
  return {
    candidateAt10: ['basic-doc:rev:P1'],
    selectedIds: ['basic-doc:rev:P1'],
    selectedTokens: 7,
    revisionIds: ['rev'],
  }
}
