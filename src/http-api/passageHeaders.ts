export const passageHeaders = {
  ETag: {
    schema: { type: 'string' },
    description:
      'Strong representation-version and immutable identity validator.',
  },
  Vary: { schema: { type: 'string' } },
}
